# ─── Docker Environment ──────────────────────
# Copy this to .env and customize, or override via shell environment.

# Ports
BACKEND_PORT=3000
FRONTEND_PORT=5173

# Backend
NODE_ENV=development
DATABASE_URL=file:./dev.db
FRONTEND_URL=http://localhost:${FRONTEND_PORT}

# Security
RESUME_FIELD_ENCRYPTION_KEY=<ENC:AES-256>
